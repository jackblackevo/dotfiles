# True color (RGB): no terminal-features override is set here on purpose.
# Alacritty always exports COLORTERM=truecolor for the shell, and tmux >= 3.6
# promotes a COLORTERM of truecolor/24bit to the RGB feature on its own. The
# `alacritty` terminfo entry itself carries no RGB flag, so COLORTERM is the
# actual trigger (not terminfo). Contrast Ubuntu/.tmux.conf: Windows Terminal sets
# TERM=xterm-256color (no RGB flag) and leaves COLORTERM unset under WSL, so
# there the feature must be declared explicitly.
# https://github.com/tmux/tmux/blob/3.6/CHANGES (3.5a -> 3.6: "Set and check COLORTERM as a hint for RGB colour")
# https://github.com/alacritty/alacritty/blob/master/alacritty_terminal/src/tty/mod.rs (sets COLORTERM=truecolor)

set-option -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

bind-key C-r rotate-window
unbind-key C-o

# https://neovim.io/doc/user/faq/#_esc-in-tmux-or-gnu-screen-is-delayed
# https://github.com/tmux/tmux/wiki/Getting-Started#list-of-useful-options
# https://github.com/tmux/tmux/wiki/Modifier-Keys#the-escape-key
# https://github.com/tmux/tmux/wiki/FAQ#what-is-the-escape-time-option-is-zero-a-good-value
#
# tmux >= 3.5 already defaults to 10ms; explicit here for compatibility with older versions
# See: https://github.com/tmux/tmux/blob/3.5/CHANGES
#
# https://man.openbsd.org/tmux#set-option
# -g: session global flag; redundant for server options, paired with -s following Neovim's FAQ
# -s: server option flag; escape-time has always been server-scoped
# set-option -gs escape-time 10

set-option -g history-limit 50000

# https://neovim.io/doc/user/options/#'autoread'
# https://github.com/tmux/tmux/wiki/Getting-Started#list-of-useful-options
set-option -g focus-events on

# https://dev.to/iggredible/the-easy-way-to-copy-text-in-tmux-319g
set-option -gw mode-keys vi

# https://github.com/tmux/tmux/wiki/Getting-Started#copy-mode-key-bindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel
