Host *
  # Re-add a key (and cache its passphrase) into the running ssh-agent on
  # use when it is missing, e.g. after the passphrase prompt at shell
  # startup (see ~/.zshrc) was skipped
  # See: https://man.openbsd.org/ssh_config.5#AddKeysToAgent
  AddKeysToAgent yes
